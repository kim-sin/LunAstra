diff --git a/luna_astra/codemap.py b/luna_astra/codemap.py
index e263220..8b71889 100644
--- a/luna_astra/codemap.py
+++ b/luna_astra/codemap.py
@@ -156,6 +156,7 @@ class CodeMap:
 
     def build(self, max_seconds: float | None = None):
         start=time.monotonic(); prior={}
+        if max_seconds is not None and max_seconds < 0: raise HarnessError('invalid time budget')
         if self.path.is_file():
             try:
                 obj=load_json(self.path)
@@ -163,9 +164,9 @@ class CodeMap:
             except (OSError,ValueError,TypeError): prior={}
         base_patterns,ignore_partial=_ignore_rules(self.root)
         patterns=[('',p) for p in base_patterns]
-        files={}; skipped=[]; errors=[]; total=0; examined=0; hits=0; parsed=0; truncated=False
-        stop=False
-        for current,dirs,names in os.walk(self.root,followlinks=False):
+        files={}; skipped=[]; errors=[]; total=0; examined=0; hits=0; parsed=0; truncated=bool(max_seconds is not None and max_seconds==0)
+        stop=truncated
+        for current,dirs,names in (() if stop else os.walk(self.root,followlinks=False)):
             if max_seconds is not None and time.monotonic()-start>max_seconds: truncated=True;break
             dirs.sort(); names.sort()
             if Path(current)!=self.root:
diff --git a/luna_astra/gitspace.py b/luna_astra/gitspace.py
index c3615f0..c4a65e4 100644
--- a/luna_astra/gitspace.py
+++ b/luna_astra/gitspace.py
@@ -11,7 +11,7 @@ from pathlib import Path
 import shutil
 import subprocess
 import time
-from .util import HarnessError, atomic_write, file_hash, inside, load_json, no_symlinks, write_json
+from .util import HarnessError, atomic_write, file_hash, inside, load_json, no_symlinks, write_json, same_path
 from .coordination import normalized
 
 class Workspaces:
@@ -49,14 +49,27 @@ class Workspaces:
             if info['root']!=str(root) or info['paths']!=paths:raise HarnessError('worktree ticket identity mismatch')
             return info
         top=Path(self._git(root,'rev-parse','--show-toplevel').stdout.decode().strip()).resolve()
-        if top!=root:raise HarnessError('select the Git repository root for isolated implementation')
+        if not same_path(top,root):raise HarnessError('select the Git repository root for isolated implementation')
         if self._git(root,'status','--porcelain=v1','-z','--untracked-files=all').stdout:
             raise HarnessError('source checkout has uncommitted files: preserve them; use one writer rather than an incomplete worktree')
-        # Checkout filters may execute commands or fetch data. Do not start them implicitly.
-        filters=self._git(root,'config','--get-regexp',r'^filter\..*\.(smudge|process)$',check=False)
-        if filters.returncode==0 and filters.stdout.strip():raise HarnessError('checkout filters configured: use the existing workspace without starting filters')
-        if filters.returncode not in (0,1):raise HarnessError('cannot inspect checkout filters')
-        files=self._names(root,'ls-files','-z');snapshot={};modes={};total=0
+        files=self._names(root,'ls-files','-z')
+        # A globally installed driver (for example Git LFS on hosted runners) is
+        # harmless until this repository opts into it. Reject repository-local
+        # filter commands, and also reject tracked paths with an active filter
+        # attribute, without ever invoking the filter itself.
+        local_filters=self._git(root,'config','--local','--get-regexp',r'^filter\..*\.(smudge|process)$',check=False)
+        if local_filters.returncode==0 and local_filters.stdout.strip():
+            raise HarnessError('checkout filters configured: use the existing workspace without starting filters')
+        if local_filters.returncode not in (0,1):raise HarnessError('cannot inspect checkout filters')
+        if files:
+            raw=b''.join(name.encode('utf-8')+b'\0' for name in files)
+            attrs=self._git(root,'check-attr','-z','--stdin','filter',data=raw,check=False)
+            if attrs.returncode not in (0,1):raise HarnessError('cannot inspect checkout filter attributes')
+            fields=attrs.stdout.decode('utf-8','strict').split('\x00')
+            triples=[fields[i:i+3] for i in range(0,len(fields)-2,3)]
+            active=[path for path,attr,value in triples if attr=='filter' and value not in {'unspecified','unset'}]
+            if active:raise HarnessError('checkout filter attributes configured: use the existing workspace without starting filters')
+        snapshot={};modes={};total=0
         if len(files)>10000:raise HarnessError('checkout snapshot budget exceeded; prefer read-only helpers')
         for name in files:
             p=inside(root,name)
diff --git a/luna_astra/hooks.py b/luna_astra/hooks.py
index a68cddc..1ec7ff7 100644
--- a/luna_astra/hooks.py
+++ b/luna_astra/hooks.py
@@ -6,7 +6,7 @@ import sys
 import time
 from pathlib import Path
 from . import __version__, __build__
-from .util import HarnessError, strict_json, digest, canonical, json_hash, no_symlinks
+from .util import HarnessError, strict_json, digest, canonical, json_hash, no_symlinks, same_path
 from .store import Store, evidence_directory
 from .coordination import Coordinator, Conflict, edit_paths, assigned_edit_input
 from .codemap import CodeMap
@@ -113,8 +113,8 @@ class Hooks:
         key,role,root=identity(event)
         if alias and alias.get('model')==event['model'] and alias.get('key') and event['session_id']==alias['parent']:
             known=store.get(alias['key'],'meta',{})
-            legitimate={known.get('workspace'),known.get('assigned_workspace')}
-            if str(root) in legitimate:key=alias['key']
+            legitimate=[p for p in (known.get('workspace'),known.get('assigned_workspace')) if isinstance(p,str)]
+            if any(same_path(root,p) for p in legitimate):key=alias['key']
         if kind=='SubagentStart':store.put('__agent_alias__',event['agent_id'],{'parent':event['session_id'],'key':key,'model':event['model']})
         store=Store(self.state); meta=store.get(key,'meta')
         if meta is not None and not isinstance(meta,dict):raise HarnessError('invalid persisted session metadata')
diff --git a/luna_astra/jobs.py b/luna_astra/jobs.py
index 9bc31bc..05bec47 100644
--- a/luna_astra/jobs.py
+++ b/luna_astra/jobs.py
@@ -52,13 +52,14 @@ class Jobs:
             db.execute('INSERT INTO kv VALUES(?,?,?)',(self.key,'job:'+job_id,canonical(job)))
             db.execute("DELETE FROM kv WHERE scope=? AND name='finish_generation'",(self.key,))
         argv=[sys.executable,str(self.package/'luna.py'),'--state',str(self.store.directory),'--session',self.key,'job-worker',job_id]
-        logdir=self.store.directory/'sessions'/self.key/'job-launch';no_symlinks(logdir);logdir.mkdir(parents=True,exist_ok=True)
         try:
-            with (logdir/(job_id+'.log')).open('xb') as log:
-                options={'stdin':subprocess.DEVNULL,'stdout':log,'stderr':log,'close_fds':True,'shell':False}
-                if os.name=='nt':options['creationflags']=subprocess.DETACHED_PROCESS|subprocess.CREATE_NEW_PROCESS_GROUP
-                else:options['start_new_session']=True
-                process=subprocess.Popen(argv,**options)
+            # Check stdout/stderr are already recorded by Evidence.run. Avoid a second
+            # inherited log handle here: on Windows it can keep temporary/state folders
+            # locked for a brief interval after the controller has recorded FINISHED.
+            options={'stdin':subprocess.DEVNULL,'stdout':subprocess.DEVNULL,'stderr':subprocess.DEVNULL,'close_fds':True,'shell':False}
+            if os.name=='nt':options['creationflags']=subprocess.DETACHED_PROCESS|subprocess.CREATE_NEW_PROCESS_GROUP
+            else:options['start_new_session']=True
+            process=subprocess.Popen(argv,**options)
             # Reap only this owned child when it exits; never kill or wait-block the caller.
             threading.Thread(target=process.wait,daemon=True,name="lunastra-check-reaper").start()
             with self.store.db(True) as db:
diff --git a/luna_astra/util.py b/luna_astra/util.py
index 3831835..af44064 100644
--- a/luna_astra/util.py
+++ b/luna_astra/util.py
@@ -43,6 +43,16 @@ def no_symlinks(path: Path) -> None:
                                   or getattr(candidate.lstat(), "st_file_attributes", 0) & 0x400):
             raise HarnessError(f"junction is not allowed here: {candidate}")
 
+def same_path(a: Path | str, b: Path | str) -> bool:
+    """Compare filesystem identity without depending on Windows 8.3 spelling."""
+    a = Path(a); b = Path(b)
+    try:
+        if a.exists() and b.exists():
+            return os.path.samefile(a, b)
+    except OSError:
+        pass
+    return os.path.normcase(os.path.abspath(a)) == os.path.normcase(os.path.abspath(b))
+
 def inside(root: Path, relative: str, *, allow_root: bool = False) -> Path:
     if not isinstance(relative, str) or not relative or "\x00" in relative:
         raise HarnessError("path must be a nonempty relative string")
@@ -52,10 +62,15 @@ def inside(root: Path, relative: str, *, allow_root: bool = False) -> Path:
         raise HarnessError(f"unsafe relative path: {relative!r}")
     if relative in (".", "./") and not allow_root:
         raise HarnessError("explicit source/test directories required; whole-root scan is not implicit")
-    target = root / p
+    # Keep the caller's absolute spelling (notably Windows 8.3 paths) after
+    # verifying the resolved target cannot escape the resolved root. This avoids
+    # treating two spellings of the same directory as different workspaces.
+    lexical_root = Path(os.path.abspath(root))
+    target = lexical_root / p
     no_symlinks(target)
-    target = target.resolve()
-    if not target.is_relative_to(root.resolve()):
+    resolved_root = lexical_root.resolve()
+    resolved_target = target.resolve()
+    if not resolved_target.is_relative_to(resolved_root):
         raise HarnessError("path escaped workspace")
     return target
 
diff --git a/tests/test_codex_contract.py b/tests/test_codex_contract.py
index 9421e30..b27c3f8 100644
--- a/tests/test_codex_contract.py
+++ b/tests/test_codex_contract.py
@@ -249,7 +249,7 @@ class ExecutionRoutingTests(unittest.TestCase):
 
     def test_real_process_uses_assigned_directory(self):
         result=worker_exec(self.meta,[sys.executable,'-c','import os;print(os.getcwd())'])
-        self.assertEqual(result['stdout'].strip(),str(self.tree));self.assertEqual(result['exit_code'],0)
+        self.assertTrue(os.path.samefile(result['stdout'].strip(),self.tree));self.assertEqual(result['exit_code'],0)
 
     def test_relative_write_does_not_touch_caller_directory(self):
         result=worker_exec(self.meta,[sys.executable,'-c',"from pathlib import Path;Path('result.txt').write_text('owned')"])
diff --git a/tools/validate.py b/tools/validate.py
index a3e4013..383cbc5 100644
--- a/tools/validate.py
+++ b/tools/validate.py
@@ -37,9 +37,15 @@ def main():
         suite=unittest.defaultTestLoader.discover(str(ROOT/'tests'),pattern=a.pattern)
         result=unittest.TextTestRunner(stream=Tee(log),verbosity=2).run(suite)
     after=fingerprint()
+    # These tests exercise POSIX-only launcher/executable-mode semantics. On
+    # Windows they are intentionally inapplicable; every other skip remains a
+    # release failure so a new or accidental skip cannot silently pass CI.
+    allowed_skip_reasons={'POSIX launcher contract','POSIX executable mode'} if sys.platform.startswith('win') else set()
+    unexpected_skips=[(test,reason) for test,reason in result.skipped if reason not in allowed_skip_reasons]
     obj={'pattern':a.pattern,'tests_run':result.testsRun,'failures':len(result.failures),'errors':len(result.errors),
-         'skipped':len(result.skipped),'expected_failures':len(result.expectedFailures),'unexpected_successes':len(result.unexpectedSuccesses),
-         'successful':result.wasSuccessful() and before==after and result.testsRun>0 and not result.skipped and not result.expectedFailures,
+         'skipped':len(result.skipped),'allowed_skips':len(result.skipped)-len(unexpected_skips),'unexpected_skips':len(unexpected_skips),
+         'expected_failures':len(result.expectedFailures),'unexpected_successes':len(result.unexpectedSuccesses),
+         'successful':result.wasSuccessful() and before==after and result.testsRun>0 and not unexpected_skips and not result.expectedFailures,
          'wall_seconds':time.time()-start,'started_at_unix':start,'source_before_sha256':before,
          'source_after_sha256':after,'source_unchanged':before==after,'actual_model_runs':0,
          'scope':'Local software tests. Synthetic hook events do not certify native Codex or model quality.'}
