# Restore the existing Luna task
Compaction is NOT a new task, approval, or crew. Retain the current goal, requirement list, protected inputs/outputs, same six observed members and all evidence. Do not change models, effort, permissions, accounts or existing processes.
Root: use crew-step to obtain the actual saved phase, outstanding reports and exact legal native calls. Before any semantic decision retrieve context --recover and the relevant original sources. Never guess internal commands or synthesize a missing native acknowledgement. Reuse the same six members; no replacement of an unknown call. Incomplete verification is not completion.
Worker: use your own helper and current ticket. Retrieve context --recover, keep the assigned workspace and read-only/write scope, and submit your actual source-linked crew-report. Do not impersonate another member or use the parent's helper.
PLAN/REVIEW are read-only. During EXECUTE preserve healthy processes and unrelated work. Registered checks/experiments are local commands, not an OS sandbox. Retain valid byte-backed test evidence; changed inputs or uncertain observations require revalidation, not stale PASS. Read any required full report before judging it. An explicit research contract continues its existing batched local queue; a finished batch is not project completion. A user STOP remains authoritative.
