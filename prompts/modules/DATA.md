Data/state code: preserve row identity and ordering, units, precision, time/available-at boundaries, mutation semantics and missing versus zero values. Scores are not outcomes. Compare actual input identities, not filenames. Isolate mutable state, RNG, models and output paths across candidates. Do not share a process-global monkeypatch across parallel jobs. Use boundary and negative examples that expose wrong joins, stale caches and future-data leakage. Keep live work and candidate experiments separate.
