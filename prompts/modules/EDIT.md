Before editing: confirm the affected implementation and existing helper, caller and test. Preserve the supplied method and all protected paths. Change only assigned files. The hook coordinates recognized structured patch/edit calls, not arbitrary shell or external writes. Use explicit claim/release for multi-command editing; an ownership conflict requires an isolated patch or caller coordination, not bypassing the guard. Existing checks may be reused only while their dependency identities still match.
